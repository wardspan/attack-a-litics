# Frontend Environment Variables

# Backend API URL for client-side requests
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Backend API URL for server-side requests
BACKEND_URL=http://localhost:8000

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Production URLs (update these for your deployment)
# NEXT_PUBLIC_BACKEND_URL=https://attack-a-litics-production.up.railway.app
# BACKEND_URL=https://attack-a-litics-production.up.railway.app

# Analytics
# Vercel Analytics is enabled by default when deployed on Vercel
# No additional configuration needed for basic page views
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX (if you want to add Google Analytics later)
# MIXPANEL_TOKEN=your-mixpanel-token (if you want to add Mixpanel later)

# Error Monitoring (if you add Sentry later)
# SENTRY_DSN=https://your-sentry-dsn-here